%article
  - haml_tag heading do
    %a{ :href => article.abspath }= widont(article.heading)
  - if article.summary.nil? || article.summary.empty?
    ~ article.body
  - else
    ~ article.summary
    %p.read-more
      %a{ :href => article.abspath }= article.read_more
